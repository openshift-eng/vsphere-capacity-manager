---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: resourcerequests.vspherecapacitymanager.splat.io
spec:
  group: vspherecapacitymanager.splat.io
  names:
    kind: ResourceRequest
    listKind: ResourceRequestList
    plural: resourcerequests
    singular: resourcerequest
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ResourceRequest defines the resource requirements for a CI job
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ResourceRequestSpec defines the specification for a resource
              request
            properties:
              memory:
                default: 96
                description: Memory is the amount of memory in GB defaults to 96 which
                  is a typical memory consumption for a 3 control plane node cluster
                  with 3 compute nodes.
                type: integer
              networks:
                default: 1
                description: Networks is the number of networks requested
                type: integer
              required-pool:
                description: RequiredPool when configured, this lease can only be
                  scheduled in the required pool.
                type: string
              storage:
                default: 720
                description: Storage is the amount of storage in GB defaults to 720
                  which is a typical storage consumption for a 3 control plane node
                  cluster with 3 compute nodes.
                type: integer
              vcenters:
                default: 1
                description: VCenters is the number of vCenters
                type: integer
              vcpus:
                default: 24
                description: VCpus is the number of virtual CPUs defaults to 24 which
                  is a typical vCPU count for a 3 control plane node cluster with
                  3 compute nodes.
                type: integer
            type: object
          status:
            properties:
              leases:
                description: Leases is the list of leases assigned to this resource
                items:
                  description: Lease represents the definition of resources allocated
                    for a resource pool
                  properties:
                    apiVersion:
                      description: 'APIVersion defines the versioned schema of this
                        representation of an object. Servers should convert recognized
                        schemas to the latest internal value, and may reject unrecognized
                        values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
                      type: string
                    kind:
                      description: 'Kind is a string value representing the REST resource
                        this object represents. Servers may infer this from the endpoint
                        the client submits requests to. Cannot be updated. In CamelCase.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
                      type: string
                    metadata:
                      type: object
                    spec:
                      description: LeaseSpec defines the specification for a lease
                      type: object
                    status:
                      description: LeaseStatus defines the status for a lease
                      properties:
                        boskos-lease-id:
                          type: string
                        leased-at:
                          type: string
                        memory:
                          description: Memory is the amount of memory in GB allocated
                            for this lease
                          type: integer
                        pool:
                          type: string
                        port-groups:
                          items:
                            description: Network
                            properties:
                              cidr:
                                type: integer
                              cidrIPv6:
                                type: integer
                              dhcpEndLocation:
                                type: integer
                              dnsServer:
                                type: string
                              gateway:
                                type: string
                              gatewayipv6:
                                type: string
                              ipAddresses:
                                items:
                                  type: string
                                type: array
                              ipv6prefix:
                                type: string
                              linkLocalIPv6:
                                type: string
                              machineNetworkCidr:
                                type: string
                              mask:
                                type: string
                              network:
                                type: string
                              priority:
                                type: integer
                              startIPv6Address:
                                type: string
                              stopIPv6Address:
                                type: string
                              vifIPv6Address:
                                type: string
                              vifIpAddress:
                                type: string
                              virtualcenter:
                                type: string
                            required:
                            - cidr
                            - cidrIPv6
                            - dhcpEndLocation
                            - dnsServer
                            - gateway
                            - gatewayipv6
                            - ipAddresses
                            - ipv6prefix
                            - linkLocalIPv6
                            - machineNetworkCidr
                            - mask
                            - network
                            - priority
                            - startIPv6Address
                            - stopIPv6Address
                            - vifIPv6Address
                            - vifIpAddress
                            - virtualcenter
                            type: object
                          type: array
                        storage:
                          description: Storage is the amount of storage in GB allocated
                            for this lease
                          type: integer
                        vcpus:
                          description: VCpus is the number of virtual CPUs allocated
                            for this lease
                          type: integer
                      type: object
                  type: object
                type: array
              phase:
                description: Phase is the current phase of the resource request
                type: string
              state:
                description: State is the current state of the resource request
                type: string
            required:
            - leases
            - phase
            - state
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}

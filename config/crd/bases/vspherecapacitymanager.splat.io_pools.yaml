---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.11.1
  creationTimestamp: null
  name: pools.vspherecapacitymanager.splat.io
spec:
  group: vspherecapacitymanager.splat.io
  names:
    kind: Pool
    listKind: PoolList
    plural: pools
    singular: pool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.vcpus-available
      name: vCPUs
      type: string
    - jsonPath: .status.memory-available
      name: Memory(GB)
      type: string
    - jsonPath: .status.datastore-available
      name: Storage(GB)
      type: string
    - jsonPath: .status.network-available
      name: Networks
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: Pool defines a pool of resources defined available for a given
          vCenter, cluster, and datacenter
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PoolSpec defines the specification for a pool
            properties:
              cluster:
                description: Cluster cluster associated with this pool
                type: string
              datacenter:
                description: Datacenter associated with this pool
                type: string
              datastore:
                description: Datastore datastore associated with this pool
                type: string
              exclude:
                description: Exclude when true, this pool is excluded from the default
                  pools. This is useful if a job must be scheduled to a specific pool
                  and that pool only has limited capacity.
                type: boolean
              memory:
                description: Memory is the amount of memory in GB
                type: integer
              server:
                description: Server the server that provisions resources for the pool
                type: string
              storage:
                description: Storage is the amount of storage in GB
                type: integer
              vcpus:
                description: VCpus is the number of virtual CPUs
                type: integer
            required:
            - cluster
            - datacenter
            - datastore
            - exclude
            - memory
            - server
            - storage
            - vcpus
            type: object
          status:
            description: PoolStatus defines the status for a pool
            properties:
              active-port-groups:
                description: ActivePortGroups is the list of port groups that are
                  currently in use
                items:
                  description: Network
                  properties:
                    cidr:
                      description: Cidr represents the IPv4 network mask.
                      type: integer
                    cidrIPv6:
                      description: CidrIPv6 represents the IPv6 network mask.
                      type: integer
                    dhcpEndLocation:
                      description: DhcpEndLocation represents the DHCP end location.
                      type: integer
                    dnsServer:
                      description: DnsServer represents the DNS server IP address.
                      type: string
                    gateway:
                      description: Gateway represents the gateway IP address.
                      type: string
                    gatewayipv6:
                      description: Gatewayipv6 represents the IPv6 gateway IP address.
                      type: string
                    ipAddresses:
                      description: IpAddresses represents a list of IP addresses.
                      items:
                        type: string
                      type: array
                    ipv6prefix:
                      description: Ipv6prefix represents the IPv6 prefix.
                      type: string
                    linkLocalIPv6:
                      description: LinkLocalIPv6 represents the link local IPv6 address.
                      type: string
                    machineNetworkCidr:
                      description: MachineNetworkCidr represents the machine network
                        CIDR.
                      type: string
                    mask:
                      description: Mask represents the network mask.
                      type: string
                    network:
                      description: Network represents the network name.
                      type: string
                    priority:
                      description: Priority represents the priority of the network.
                      type: integer
                    startIPv6Address:
                      description: StartIPv6Address represents the start IPv6 address
                        for DHCP.
                      type: string
                    stopIPv6Address:
                      description: StopIPv6Address represents the stop IPv6 address
                        for DHCP.
                      type: string
                    vifIPv6Address:
                      description: VifIPv6Address represents the VIF IPv6 address.
                      type: string
                    vifIpAddress:
                      description: VifIpAddress represents the VIF IP address.
                      type: string
                    virtualcenter:
                      description: Virtualcenter represents the virtual center server.
                      type: string
                  type: object
                type: array
              datastore-available:
                description: StorageAvailable is the amount of storage in GB available
                  in the pool
                type: integer
              initialized:
                description: Initialized is true when the pool has been initialize
                type: boolean
              leases:
                description: Leases is the list of leases assigned to this pool
                items:
                  description: TypedLocalObjectReference contains enough information
                    to let you locate the typed referenced object inside the same
                    namespace.
                  properties:
                    apiGroup:
                      description: APIGroup is the group for the resource being referenced.
                        If APIGroup is not specified, the specified Kind must be in
                        the core API group. For any other third-party types, APIGroup
                        is required.
                      type: string
                    kind:
                      description: Kind is the type of resource being referenced
                      type: string
                    name:
                      description: Name is the name of resource being referenced
                      type: string
                  required:
                  - kind
                  - name
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              memory-available:
                description: MemoryAvailable is the amount of memory in GB available
                  in the pool
                type: integer
              network-available:
                description: Networks is the number of networks available in the pool
                type: integer
              port-groups:
                description: PortGroups is the list of port groups available in the
                  pool
                items:
                  description: Network
                  properties:
                    cidr:
                      description: Cidr represents the IPv4 network mask.
                      type: integer
                    cidrIPv6:
                      description: CidrIPv6 represents the IPv6 network mask.
                      type: integer
                    dhcpEndLocation:
                      description: DhcpEndLocation represents the DHCP end location.
                      type: integer
                    dnsServer:
                      description: DnsServer represents the DNS server IP address.
                      type: string
                    gateway:
                      description: Gateway represents the gateway IP address.
                      type: string
                    gatewayipv6:
                      description: Gatewayipv6 represents the IPv6 gateway IP address.
                      type: string
                    ipAddresses:
                      description: IpAddresses represents a list of IP addresses.
                      items:
                        type: string
                      type: array
                    ipv6prefix:
                      description: Ipv6prefix represents the IPv6 prefix.
                      type: string
                    linkLocalIPv6:
                      description: LinkLocalIPv6 represents the link local IPv6 address.
                      type: string
                    machineNetworkCidr:
                      description: MachineNetworkCidr represents the machine network
                        CIDR.
                      type: string
                    mask:
                      description: Mask represents the network mask.
                      type: string
                    network:
                      description: Network represents the network name.
                      type: string
                    priority:
                      description: Priority represents the priority of the network.
                      type: integer
                    startIPv6Address:
                      description: StartIPv6Address represents the start IPv6 address
                        for DHCP.
                      type: string
                    stopIPv6Address:
                      description: StopIPv6Address represents the stop IPv6 address
                        for DHCP.
                      type: string
                    vifIPv6Address:
                      description: VifIPv6Address represents the VIF IPv6 address.
                      type: string
                    vifIpAddress:
                      description: VifIpAddress represents the VIF IP address.
                      type: string
                    virtualcenter:
                      description: Virtualcenter represents the virtual center server.
                      type: string
                  type: object
                type: array
              vcpus-available:
                description: VCPUsAvailable is the number of vCPUs available in the
                  pool
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
